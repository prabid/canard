{
  "name": "canard-demo",
  "version": "0.0.3",
  "private": true,
  "main": "src/index.js",
  "dependencies": {
    "express": "^4.17.0",
    "canard-client": "^0.0.3",
    "canard-server": "^0.0.3",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-router-dom": "^5.0.0",
    "react-scripts": "3.0.1",
    "socket.io-client": "^2.2.0"
  },
  "scripts": {
    "start": "cross-env PORT=3000 react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "postinstall": "yarn link canard-server & yarn link canard-client",
    "start:server": "nodemon src/server"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "proxy": "http://localhost:8080",
  "devDependencies": {
    "cross-env": "^6.0.3",
    "nodemon": "^1.19.0"
  }
}
